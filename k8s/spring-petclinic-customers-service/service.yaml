apiVersion: v1
kind: Service
metadata:
  name: customers-service
  namespace: spring-petclinic
  labels:
    app: customers-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  selector:
    app: customers-service