apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  namespace: spring-petclinic
  labels:
    app: discovery-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8761
      targetPort: 8761
  selector:
    app: discovery-server