apiVersion: v1
kind: Service
metadata:
  name: visits-service
  namespace: spring-petclinic
  labels:
    app: visits-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  selector:
    app: visits-service